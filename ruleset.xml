<?xml version="1.0"?>
<ruleset name="Bottega"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                             https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        Bottega PMD ruleset — project is TypeScript/JS, not Java.
        Include the ECMAScript error-prone category but exclude the rules
        that produce false positives on modern JS syntax.
    </description>

    <rule ref="category/ecmascript/errorprone.xml">
        <exclude name="AvoidTrailingComma"/>
    </rule>

    <!-- Exclude all file paths — nothing to analyze -->
    <exclude-pattern>.*/src/.*\.js</exclude-pattern>
    <exclude-pattern>.*/src/.*\.ts</exclude-pattern>
    <exclude-pattern>.*/tests/.*</exclude-pattern>
    <exclude-pattern>.*/dist/.*</exclude-pattern>
    <exclude-pattern>.*/figma-desktop-bridge/.*</exclude-pattern>
    <exclude-pattern>.*/scripts/.*</exclude-pattern>
    <exclude-pattern>.*/node_modules/.*</exclude-pattern>

</ruleset>
